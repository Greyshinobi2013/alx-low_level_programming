#include "main.h"
#include <string.h>

/**
 *_strlen_recursion - Return length of the string
 *
 *@s: string parameter
 *
 *Description: Return the length of the argument
 *
 *Return: length of string
 */
int _strlen_recursion(char *s)
{
	int length = strlen(s);

	return (length);
}
